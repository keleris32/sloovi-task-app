// Task Action Types
export const actionType = Object.freeze({
  loading: 'loading',
  success: 'success',
  error: 'error',
});

export const allTasksActionType = Object.freeze({
  loading: 'loading_tasks',
  success: 'success_tasks',
  error: 'error_tasks',
});

export const usersActionType = Object.freeze({
  loading: 'loading_users',
  success: 'success_users',
  error: 'error_users',
});
